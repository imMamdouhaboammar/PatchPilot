@GitHub

Work exclusively on `<owner/repository>` in PR-only mode.

Refresh the default branch, repository instructions, architecture, recent commits, issues, active and recently merged PRs, CI, tests, and backlog. Build a ranked inventory of verified bugs and useful improvements. Avoid work already active elsewhere.

Create focused branches and separate PRs for separate themes. Implement complete, test-backed changes and use meaningful atomic commits. Commit counts are cumulative targets, never a reason for filler or artificial splitting.

Do not merge, publish, tag, change versions, or push to the default branch. End with PR links, branch names, commit counts, exact validation, CI state, risks, and recommended independent review order.
