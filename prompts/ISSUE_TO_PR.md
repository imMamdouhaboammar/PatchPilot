@GitHub

Work exclusively on `<owner/repository>` and convert issue `<issue number or URL>` into a focused PR.

First verify the issue against current code, recent commits, active PRs, tests, and acceptance criteria. Identify the root cause, direct consumers, and regression case. Create a branch from the latest default-branch SHA, implement the smallest complete fix, add tests, run available validation, and open a documented PR.

Do not merge or close the issue automatically. If the issue is stale, already fixed, duplicated, or unsafe to implement, report evidence instead of creating unnecessary changes.
