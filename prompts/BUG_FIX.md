@GitHub

Work exclusively on `<owner/repository>` in PR-only mode.

Bug: `<describe observed behavior and expected behavior>`

Inspect repository instructions, the exact failing path, callers, state, types, tests, recent changes, related issues and PRs, and CI. Confirm the root cause before writing. Add a regression test where possible, implement a surgical fix, run the relevant checks, review remote drift, and open a focused PR.

Do not perform unrelated cleanup. Do not claim the bug is fixed unless the changed behavior is supported by executed tests or clearly marked independent verification steps.
