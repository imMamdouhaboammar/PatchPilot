@GitHub

Work exclusively on `<owner/repository>` and repair pull request `<PR number or URL>` without merging it.

Read the PR body, changed files, commits, review comments, checks, related issue, base-branch movement, and repository instructions. Identify only the blockers and regressions within the PR theme. Update the existing branch when authorized, add or repair tests, run available validation, and update the PR description with exact results and remaining risks.

Do not broaden the PR, hide failures, force-push, or merge.
