@GitHub

Work exclusively on `<owner/repository>` in PR-only mode.

Feature: `<describe the user or developer outcome>`
Acceptance criteria:
- `<criterion>`

Inspect current architecture, similar features, data flow, types, validation, tests, docs, recent commits, active PRs, and CI. Present a concise plan and obtain confirmation only if the feature changes core architecture, public APIs, or persistent data.

Create a branch from the latest default head. Implement one complete vertical slice with real wiring, errors, tests, and docs. Do not add placeholders, fake data, inactive controls, or disconnected interfaces. Open the PR and leave merge authority to the independent reviewer.
