interface:
  display_name: "PatchPilot"
  short_description: "Repo-specific patterns and workflows for PatchPilot"
  default_prompt: "Use the PatchPilot repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true